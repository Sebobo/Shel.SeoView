prototype(Shel.SeoView:MetaKeywords) < prototype(TYPO3.TypoScript:Tag) {
    tagName = 'section'
    attributes.class = 'seo-view__meta-keywords'
    
    content = TYPO3.TypoScript:Array {
        header = TYPO3.TypoScript:Tag {
            tagName = 'h2'
            content = 'Meta keywords'
        }

        content = TYPO3.TypoScript:Tag {
            tagName = 'p'
            keywords = ${q(node).property('metaKeywords')}
            content = ${String.isBlank(this.keywords) ? 'No keywords defined' : this.keywords}
        }
    }
}
